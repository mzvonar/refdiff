# refdiff — Handoff (2026-09-04, current)

Design-vs-implementation harness. Repo `~/development/refdiff` on the `ai-mzvonar3` devbox (Mato's
Mac: `~/Development/refdiff`), branch **`main`**, pnpm workspace (pnpm 10.33), TypeScript/ESM,
Node ≥22. **This is the canonical latest handoff**; it supersedes `handoff-2026-09-03.md`, which
stays as the long-form record of sessions 17–20 — read it for the WHY behind anything named here.

## State of play — UPDATED 2026-09-04, session 21

**`refdiff-compare-mobile-toolbar` now PASSES**: 10 → 4 findings, 5 → **1** unexplained, and its
alignment reached the exact identity (`1 / 0,0`). 538 tests (335 core + 203 annotator).

| pair | findings | UNEXPLAINED | conf | note |
| --- | --- | --- | --- | --- |
| refdiff-library-desktop | 10 | 10 | 0.89 | `+0/−0` |
| refdiff-library-mobile | 8 | 8 | 1.00 | `+0/−0` |
| refdiff-compare-desktop | 69 | 9 | 0.72 | `+0/−0` — the shared fixes are inert here, which is the falsification |
| refdiff-compare-mobile | 15 | 9 | 0.97 | **OUT OF SCOPE — old layout · NOT re-measured, row is 09-03 and WILL move** |
| refdiff-compare-mobile-minimal | 25 | 18 | 0.87 | **OUT OF SCOPE — old layout · NOT re-measured, row is 09-03 and WILL move** |
| refdiff-compare-mobile-toolbar | **4** | **1** | **1.00** | **PASS** |
| refdiff-compare-mobile-toolbar-ghost | 90 | 39 | **0.46** | `+2/−7`, no regressions |
| refdiff-compare-desktop-ghost | 109 | 35 | 0.56 | `+0/−0` |

**Set 330, of which 129 unexplained · 201 explained** (138 comp rail row order, 40 comp mark
numbering, 23 canvas zoom divergence) — but **two of the eight rows are stale by decision**, so
that total is "330 with two rows pre-change", not a clean measurement. Six pairs were re-run to
`+0/−0` this session; the ghost pair settled at `+2/−7` with **no REGRESSION line**.

### What session 21 changed (all measured before and after)

| # | fix | measured |
| --- | --- | --- |
| 1 | `.seg.seg-p` flatten kept `border-radius:8px`; `paintsDecoration` is bg OR border OR **radius > 0**, so a container painting an invisible radius still emits | killed the `extra-element` surface 215×21 at (12,123) |
| 2 | `#seg-variant button` → `padding:3px 9px; line-height:15px` (the comp's segment is 219×**27**, not 32) | killed `size`, `spacing` AND the `alignment` note in one |
| 3 | `.delta-strip .review` → `line-height:15px` (both comps render it 25 tall; the app's global 1.4 made it 26.09) | phone strip 66.09 → 65; desktop `+0/−0` |
| 4 | `pageShadow(dpr)` on the two page images (`parts/Artboard *` carry `0 4px 30px rgba(0,0,0,.35)`; a screenshot cannot contain its own shadow) | the two shadow ramps are now byte-identical |
| 5 | ghost switch label in its OWN span + `.gpill .gsw { line-height:14px }` | `"View design"` is a paintless leaf on both sides; the button surface is 98×20 radius 10 on both |

**Design ask 5 is ANSWERED and must not be sent.** The `scale y 0.99937 / offset y 0.5` was never
the comp's 44px header — it was the header SEGMENT being 5px too tall. Both headers render 45, so
`calc(44px + 1px)` is correct. `refdiff.bindings.md` carries the correction beside the superseded
sentence.

## SCOPE RULING (Mato, 2026-09-04)

**The two OLD phone layouts are out of scope: `refdiff-compare-mobile` (the `default` layout) and
`refdiff-compare-mobile-minimal`.** Work only the NEW toolbar layout — `refdiff-compare-mobile-toolbar`
and its ghost pair. Do not spend a run on the other two, do not tune their policies, and do not read
their numbers as debt.

Open sub-question for Mato, recorded 2026-09-03 and now half-answered: those two layouts are
URL-only since the toolbar layout became the phone's single layout (`?layout=default` /
`?layout=minimal`), and each still has a pair against a real comp. **Should they be deleted
outright** — the layouts, their CSS, and their two manifest pairs? That is the tidy end state and it
is a destructive call, so it waits for an explicit yes.

## What's DONE (session 20, all on `main` — session 21's five fixes are in the table above)

- **`a3f0455` a comment is its badge until you select it** (`packages/annotator/src/render.ts`,
  `renderAnnMarks`). The comps draw a mark as a BADGE and reserve the outline for the SELECTED one
  (`RefDiff Comparison Tool.dc.html:587` findings, `:595` comments); the app's finding layer already
  did, its comment layer never had. 22 reported `shape` findings → 0, set 383 → 349.
- **`5e259f3` a comment belongs to the pane it was drawn on** — the 2026-08-28 mirror REVERSED
  (Mato: a finding is a pair, "here and missing there"; a comment is "I clicked here"). Set 349 →
  349 with 14 findings moved so each names a real cause.
- **`75a13c1` + `15374c2` the phone's one layout is the toolbar one**, and it is NOT a stored
  preference (`state.mlayout = urlLayout() || 'toolbar'`). It had shipped unreachable: the boot line
  could not read back the value its own setter persisted, and the first fix then honoured the
  `'default'` every earlier visit had written — a fresh-context probe passes both times, only a
  probe that SEEDS localStorage sees the second.
- **`8daf356` the fit solves for the canvas you can SEE** (`paneInsets`): every panel over the pane
  costs its CHEAPEST edge, so a floating pill insets the edge it hugs. Insets at 390×844: default
  `b45→b98`, minimal `0→b45`, toolbar `0→t37 b45`; desktop unchanged.
- **`51f4e25` `contentsOf`** — an excuse whose container is an ELEMENT, not a finding. Replaced the
  artboard `accepted … contents: true`, which fired on ONE pair of six by accident of pairing.
- **`d4e4263` the ghost pair swaps the pane first**, so the ghost footprint and pill are MEASURED
  rather than crop-verified.
- **`5e4405e` a finding can be EXPLAINED** — the third state beside reported and suppressed
  (`ignore.explain`, `package/verdict.ts`). See "Key facts".
- Docs: `SKILL.md`, `docs/architecture.md`, `refdiff.bindings.md` all carry these; three stale
  claims were corrected against measurement (the artboard rule fires on ONE pair not two; the comp
  has FOUR `cause` lines not two; the "two pairs quiet" line).

## What REMAINS (in order)

### 1. `refdiff-compare-mobile-toolbar` — DONE, it PASSES. One finding left and it is not ours.
The remaining `pixel-region` on the canvas (5.4% of 380×721, 172 clusters) is fully diagnosed;
`refdiff.bindings.md` carries the method and the numbers. Two causes:
- **A pure 4.0px vertical offset** — dx 0.0, no scale, the SAME 4.0 in the top and bottom halves,
  mean |Δ| 10.77 → 2.55 at that shift. It is the DECIDED `paneInsets` divergence and the arithmetic
  closes exactly: insets t37 b45, so the app's page sits **(45 − 37)/2 = 4.0px** higher than the
  comp's. **Design ask 6 now has a number on this pair too**, not only on the ghost pairs' 27.
- **The rest is the comp's live-DOM artboard against the app's PNG at 0.5265×** — glyph-edge
  antialiasing, ~5% at mean |Δ| 1%. Nothing in the app reaches it.

**A decision is waiting, and it is deliberately NOT taken:** an `explain` rule would take this pair
to 0 unexplained, but its `types` would have to include `pixel-region` over the canvas — the ONLY
channel that sees the artboard at all. That buys a cosmetic zero on a pair that already PASSES and
pays with blindness to any future drawing regression in the pane. Left reported on purpose.

### 2. `refdiff-compare-mobile-toolbar-ghost` — triaged finding by finding; one fix landed
90 findings, 39 unexplained, confidence 0.46 (x 0.99 / y 0.48) — still under the gate, so nothing
positional is readable. The alignment-independent set is now fully accounted for:
- **The badge ALPHA family (`"14"` ×4, `"5"`) is a DESIGN ASK, not drift — ask 7 below.** Not
  accepted and not changed: it is product behaviour and the comp is not silent on selection.
- **The `"2"` family (8 findings) is ONE same-text cross-pairing** — the design has four "2"s (a
  grey rail count chip, two critical badges, a purple comment badge) and the impl three; the matcher
  crossed a comment badge with a rail chip and a finding badge with a comment badge.
  `unrelatedPairing` cannot reach it (it needs disjoint tokens). Already on the futures list.
- **`"public"` / `"upload_file"` are registration artefacts** — a dimmed `.vmark` where the comp
  draws an artboard icon.
- **`"View design"` was the one genuine leaf-shape mismatch and it is FIXED** (fix 5 above). Note
  the shape of the result: the button surface is now byte-identical on both sides — 98×20, radius
  10, `rgb(91,141,239)` — and STILL reports `extra-element`, because it is 192px away and textless.
  So is the ghost PILL (both sides 304×32, radius 9, 1px dashed critical, same shadow). **On this
  pair a textless `extra-element` is evidence about the FIT; check `elements.json` for the
  counterpart before believing one.**
- `text-content` "… · 1 unsaved" is demo data.

Nothing further is workable here until the alignment registers, and its cause is design ask 1.

### Later / future reference
- **The design asks are drafted and unsent** — see §"Design asks" below. Asks 1 and 6 are worth 136
  and 27 findings across the set.
- **The comment GHOST is unbuilt and load-bearing** since the mirror went: it is the only cross-pane
  signal, and on a phone the off-pane canvas shows no comment mark at all. Needs three
  status-coloured hatch patterns beside the severity ones plus the comps' point geometry; measuring
  it needs `data-vc-step` on the comp's comment rows (ask 4a).
- **`SVG_SHAPE_CAP = 24` reports one visual layer in HALVES** — the app's finding-mark outlines are
  still invisible because that svg is over the cap.
- **`fill-opacity` / `stroke-opacity` are not folded into reported paint** (`extract.ts:243` folds
  CSS `opacity` only) — free today, a false `color` the moment such a shape pairs.
- **A same-text re-pairing the veto cannot reach**: `unrelatedPairing` needs DISJOINT tokens, so a
  finding badge "2" pairing with a comment badge "2" is invisible to it. Sibling of the long-open
  TEXTLESS veto. **Measured 2026-09-04: it is EIGHT of the ghost pair's 39 unexplained findings** —
  four design "2"s against three impl "2"s, crossed two ways (comment badge ↔ rail count chip,
  finding badge ↔ comment badge). Biggest single lever left on that pair after the alignment
  itself, and unlike the alignment it is ours to fix.
- `refdiff-compare-desktop` is at **9 unexplained** and nobody has read them row by row.
- Re-fetch the Minimal comp; a per-pair `prepare` hook to own the fixture-clock ritual.
- **Deferred WITH REASONS — do not re-litigate:** `opacity` as its own channel, `outline`, comparing
  paint servers, `borderStyle` in `presenceIdentity`.

### Needs research / open questions
- **Delete the two old phone layouts and their pairs?** (see SCOPE RULING). It got sharper this
  session: their two rows in the table above are now STALE — the shared `.delta-strip .review`
  line-height and the page shadow have moved them and nobody has measured it. Either delete them or
  re-measure them; leaving a stale row in the baseline is the worst of the three.
- **Should the toolbar pair's canvas `pixel-region` be explained?** See §1 — the trade is stated,
  the decision is not taken.
- `docs/lessons-inbox.md` has an unprocessed entry (the crop / class-collision lesson) — and a
  second one from this session: **read a crop's origin from `cropPadding = 12` CLIPPED to the
  frame, never from the finding's box.** A crop of (7.5,115) in a 390×844 frame starts at (0,103);
  getting it wrong reads as a plausible small offset rather than as an error.
- The design asks need sending. **Ask 5 is now ANSWERED by measurement and must be dropped** (it was
  the header segment, not the header); **ask 6 gained a second number** (4.0px on the toolbar pair,
  derived exactly from insets t37 b45); and there is a **new ask 7**.

## How to run

```bash
pnpm build && pnpm typecheck && pnpm test          # 538 tests: 335 core + 203 annotator

# the measured impl (read-only: the served app WRITES into what it serves)
refdiff-annotator fixtures/demo-root --serve --read-only --port 7381 --host 0.0.0.0
refdiff-annotator out/refdiff --serve --port 7382 --host 0.0.0.0        # review UI
( cd design/refdiff && python3 -m http.server 7390 --bind 127.0.0.1 )   # the comps, to hand-measure

# measure — PIN THE FIXTURE CLOCK, measure, restore.
node fixtures/make-demo-root.ts --now
refdiff compare --manifest design/refdiff.manifest.mjs --design-dir design/refdiff \
  --app-url http://127.0.0.1:7381 --out out/refdiff --pair refdiff-compare-mobile-toolbar
node fixtures/make-demo-root.ts
refdiff summary out/refdiff
```

The toolbar layout in a browser: `http://127.0.0.1:7381/?layout=toolbar#/onboarding-document-step`
(and it is the phone DEFAULT now, so a bare URL at ≤759px gives it too).

After ANY annotator edit: `pnpm --filter @refdiff/annotator build`, confirm `dist/render.js` is
newer than `src/render.ts`, and RESTART the 7381 server — its shell is rendered at start.

## Key facts / decisions

- **A finding can be EXPLAINED (2026-09-04).** `ignore.explain: [{ types, region | within: { role },
  text?, cause, reason }]` keeps the finding reported with its severity, tags it
  `explained: { cause, rule }`, groups it in the run line, and leaves it OUT of the verdict.
  `types` is the SAFETY: name only what the cause can physically produce, so `color`, `typography`,
  `border` and `text-content` in the same region stay unexplained and still fail. **It does not
  lapse by itself** the way an `accepted` rule does, so two audits watch it: every run prints any
  cause whose count MOVED against the previous run, and `refdiff summary` names any declared cause
  that matched nothing anywhere in the set. The delta is the backstop — an explained finding is
  still in it.
- **`contentsOf` (2026-09-03).** The artboard excuse names the ELEMENT, fires every run, follows a
  panned canvas, and skips a container the frame does not contain. Text is never excused (the comp
  draws its badges over the artboard).
- **The app diverges from the comps deliberately in two places, both decided:** the pane INSETS
  bound the fit and the focus zoom (the comps size from the full pane), and a comment belongs to the
  pane it was drawn on (the comps draw ordinary demo comments on both panes).
- **The comps are CONTENT-box; the app is border-box.** Every app rule copying a fixed size from a
  bordered comp box is written as the comp's number plus the border (`calc(44px + 1px)`).
- **A green pair proves the STATE matches the comp, never that a user can REACH it.** Every pair
  pins the URL and steps that put the app in that state; the default a user actually gets is
  measured by no pair unless one pins it.
- Source-of-truth pointers: `refdiff.bindings.md` (paths, ports, the measured baseline, the traps —
  READ IT FIRST), `skills/refdiff/SKILL.md` (the loop), `docs/architecture.md` → Open decisions,
  `docs/handoff-2026-09-03.md` (sessions 17–20 long form).

## Design asks (drafted 2026-09-03, NOT yet sent)

1. **Rail row order** — the comp's demo lists rows 1,2,3,7,8,9,10,11,4,5,6,12..15; refdiff lists by
   severity. **136 findings**, and it is why the ghost pairs sit under the confidence gate.
2. The **four** `cause` lines (policy excuses two; the two added with the one-sided rows cost 8).
3. The demo COMMENTS need the side they were made on (`oneSide` on c1–c3).
4. `data-vc-step` hooks: (a) the comment rows, (b) the rail-open button, (c) the pane-swap control.
5. ~~Question: does the toolbar comp's 44px header include its border?~~ **ANSWERED 2026-09-04 by
   measurement — DO NOT SEND.** The `scale y 0.99937` was the header SEGMENT (219×32 against the
   comp's 219×27), not the header. Both headers render 45; `calc(44px + 1px)` is correct and the fit
   is now the identity.
6. Question: should the comps' fit and focus respect their own floating panels, as the app's now do?
   **27 findings**, and it is the whole residue on the ghost pairs' canvas. **Also the toolbar
   pair's last finding**: measured as a pure 4.0px vertical offset of the whole page, which is
   exactly `(b45 − t37) / 2` for that layout's insets.
7. **NEW: when a finding is SELECTED, should the other marks drop to 35%?** The app dims every
   non-selected, non-comment mark (`.vmarks.has-sel .vmark:not(.sel):not(.ann) { opacity:.35 }`)
   because three marks can share a box and the neighbour drawn last covered the one you clicked. The
   comps model selection fully — outline, ghost, rail row — but set mark opacity from STATUS only
   (`ignore`/`snooze` 0.45, suppressed 0.5, else 1), with no selection term. So this is a real
   choice the designer has already had the chance to make, not an oversight: worth **5 findings ×
   2 ghost pairs**, and it is the only app-vs-comp difference on those pairs that is neither the
   alignment nor a pairing artefact.

## Env gotchas

- **No backticks anywhere in `render.ts`'s client block, comments included** — it lives in a TS
  template literal, and one backtick is an `Unclosed comment`-class build break. Bit me this session.
- **The manifest parser WHITELISTS `ignore` keys** (`readIgnore`). A new policy field it does not
  read is dropped in SILENCE — core, policy and manifest can all be correct while five pairs of six
  do not move. Every new field needs a row in `manifest.test.ts`.
- **A server restart needs the port to actually free.** Kill, poll until the port refuses, then
  start — otherwise the new process dies with EADDRINUSE and the OLD one keeps answering 200, so
  readiness checks lie. Verify by CONTENT (grep the served shell for a string you just added).
- **`pgrep -f` / `pkill -f` match the shell running them.** Kill by PID.
- **Pin the fixture clock before measuring**, regenerate without `--now` afterwards, never commit
  `--now` output.
- **A `python3` heredoc that fails does NOT stop a following `git commit`** unless they are in one
  `&&` chain. That shipped a one-of-four docs fix to main this session.
- **Compare `dist/render.js`'s mtime against `src/render.ts` before believing any measure.**
- A change to the matcher, a role name or the view RE-KEYS findings, so the delta churns exactly
  once and the next run is clean. Say so rather than chasing it.
- `out/` is gitignored; the served app WRITES into what it serves, so the measured instance is
  `--read-only` and `git checkout fixtures/` undoes an accident.
