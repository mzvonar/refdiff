# refdiff — Handoff (2026-09-02, SUPERSEDED)

> **SUPERSEDED by `handoff-2026-09-03.md` — read that first.** This file stays as the long-form
> record of sessions 17–19: the per-feature detail, the falsifications, and the measurements behind
> each decision. Its STATE lines and pair tables are frozen at 2026-09-03 morning and some are
> already stale by design — the current baseline, the current REMAINS list and the decisions taken
> since (the zoom-on-select rule, chiefly) live in the newer doc.


Design-vs-implementation harness, branch `main` since 2026-09-04 (this file says
`fix/setup-dev-cli-wrappers` below, which was true when written and is now deleted), repo
`~/development/refdiff` on the `ai-mzvonar3` devbox (Mato's Mac: `~/Development/refdiff`),
pnpm workspace (`packageManager` pins pnpm 10.33), TypeScript/ESM, Node ≥22. This is the
canonical latest handoff; it supersedes `handoff-2026-08-28.md` for everything below.

## State of play

**The GHOST language is BUILT (session 18); the six harness improvements it paid for landed on top,
and the zoom-on-select question is decided (session 19, 2026-09-03 — the pairing veto, the
pixel-region identity, the re-pairing diagnosis, `borderStyle`, the region breakdown, SVG
extraction, and selecting a finding now zooms to it).** Session 17 had it
specified and unimplemented; that job is done — the footprint, the pill, the halo and the row
chips are in `packages/annotator/src/render.ts`. The ghost itself moved three pairs
(`compare-desktop` 104 → 79, `compare-desktop-ghost` 176 → 155,
`compare-mobile-toolbar-ghost` 119 → 95, the other five `+0/−0`); session 19's core work then
moved them again, and the CURRENT numbers are the table below — read that, not these. What remains
on the ghost pairs is NOT the ghost: see "What REMAINS".

Session 17 (2026-09-02) did five things, in this order, all driven by findings rather than
by reading comp source:

1. Adopted the **Mobile Toolbar** layout (a sixth pair) and converged it to PASS.
2. Built **four new core capabilities** after two real misses the harness could not see.
3. Added **run numbering** so the delta strip reads "Run 47 vs 46".
4. Built **interaction `steps`**, because the ghost only exists after a tap.
5. Brought the **fixture to parity** with the comps' updated demo data.

**Committed, not pushed** — TRUE WHEN WRITTEN, and no longer: everything named here reached `main` on 2026-09-04 (`5e4405e`) and the branch is deleted. Everything sat on `fix/setup-dev-cli-wrappers` above `195c689`
(`fix(dev): install CLI wrappers …`, which IS pushed) — session 17's five, session 18's three,
session 19's two:

- `165c965` feat(core): surfaces, a pixel backstop, scoped policy, run numbers, steps
- `9b3b7d5` feat(annotator): the toolbar phone layout, and honest rail counts
- `3733724` feat(fixtures): the comps' ghost rows, and two step-driven pairs
- `4de8149` docs(skill): §0 — a surface that does not exist yet
- `230c4c9` docs: the 2026-09-02 handoff, the new baseline, and two recorded misses
- `9e7b691` feat(annotator): the ghost of a one-sided finding
- `63f98f1` docs(skill): the one crop a blind channel earns
- `df47ea9` docs: the ghost's measured baseline, and the zoom question it leaves
- `9933e26` feat(core): refuse a pairing we can prove wrong, and stop the delta crying wolf

Core work lands as ONE commit per session on purpose: split per feature it would not compile,
because the features share `types.ts`, `cli.ts`, `index.ts` and `SKILL.md`.
Push only when Mato asks.

Current pair table (session 18, fixture clock pinned per batch, every pair re-run to `+0/−0`
before recording — `refdiff summary out/refdiff` reproduces it). Session 17's numbers in the
last column:

| pair | findings (c/M/m) | supp | conf | align | was |
| --- | --- | --- | --- | --- | --- |
| refdiff-library-desktop | 10 (1/3/6) | 26 | 0.89 | identity | 10 |
| refdiff-compare-desktop | **89 (24/54/11)** | 56 | 0.72 | identity | 104 |
| refdiff-library-mobile | 8 (1/3/4) | 9 | 1.00 | identity | 8 |
| refdiff-compare-mobile | **19 (4/8/7)** | 24 | 0.97 | identity | 13 |
| refdiff-compare-mobile-minimal | **36 (4/22/10)** | 34 | 0.87 | identity | 31 |
| refdiff-compare-mobile-toolbar | **17 (4/4/9)** | 24 | **1.00** | 1×0.999 / 0,0.5 | 12 |
| **refdiff-compare-mobile-toolbar-ghost** | **89 (22/53/14)** | 52 | **0.49** | 1×0.994 / 0,4.2 | 119 |
| **refdiff-compare-desktop-ghost** | **115 (34/61/20)** | 60 | **0.56** | identity | 176 |

**383 findings across the set** (476 → 403 → 396 → 422 → 383). Session 19's six core changes account for
the last two moves: the pairing veto took the two ghost pairs 95 → 93 and 155 → 150, and SVG
extraction took the four non-ghost compare pairs up by 26 by making the app's own canvas overlay
visible for the first time — 14 of those are data (the comment layer), 12 are knock-on re-pairings,
and the ghost pairs did not move because their shapes land inside the accepted artboard region.
Then the zoom-on-select decision took those two ghost pairs 150 → 115 and 93 → 89 and moved nothing
else. The Library pairs have not moved through any of it. The mobile ghost pair is still under the 0.5 confidence
gate, so per SKILL.md §1a nothing positional in it is readable — its vertical anchors disagree
because the comp's rail rows are in the comp's own demo order (gap 32), not because of the ghost.

## Session 19 (2026-09-03) — six harness improvements the ghost run paid for, and one product decision

Every one of these came out of session 18's own friction, and each carries the measurement that
justified it. Core changes, so they touch every pair: the two ghost pairs churned once and settled
to `+0/−0`; the other six did not move.

**1. The matcher refuses a pairing it can PROVE wrong** (`structural/match.ts`,
`unrelatedPairing`). Geometry alone pairs a rail badge "6" with a prop line "element" 90 γ away when
the two lists are in a different order, then reports position, colour, typography, radius and
text-content about two unrelated elements. Measured before the fix: **12 such findings on
`compare-desktop-ghost`, 14 on `compare-mobile-toolbar-ghost`, 2 on `compare-desktop`** — and one of
them was the REGRESSION line that cost session 18 a ledger investigation.
The veto needs positive evidence, not merely differing text: no shared tokens, at least
`DEFAULT_UNRELATED_MIN_GAMMA` (20) apart, **and each text present somewhere on the other side** —
so both elements had a same-text partner available and this pair is not it. Both sides then report
missing/extra, which is what a different list order actually is.
Falsified per row against the corpus before it was written: it keeps every value slot
(`146%` ↔ `100%` at γ 0.5, a card count at γ 0, the Library's `Processing` ↔ `Clean` at γ 52 whose
word is nowhere on the impl side) and every token-sharing copy drift, and refuses the four real
mispairings. It is a CANDIDATE veto, not a dissolved winner, so the greedy assignment can still
give both elements their right partners — which is why the log reports only the refusals where both
ended unmatched: the Library pairs refuse two candidates and their outcome is byte-identical.
Each pass passes its own distance (γ in pass 2, the position-only `slotGamma` in pass 3, whose full
γ is dominated by the width difference the slot pass exists to forgive — a shrink-wrapped cell
against a column-wide one is 130 γ apart and still the same slot).
After: **14 → 0** on the toolbar ghost, **12 → 7** on the desktop ghost (the survivors are the two
documented conservative cases: `'missing'` ↔ `'×14'`, whose `×14` is nowhere on the design side, and
a same-anchor prop-label slot 9 px apart).

**2. A `pixel-region` is identified by its KIND, not its measured ratio** (`package/delta.ts`).
Its `actual` carries `diffRatio`/`diffPixels`/`clusters`, and the identity key canonicalised all of
them — so any twitch made the region resolve and re-introduce itself under its own id. Measured
twice in one session: a canvas box went 15.9% → 16.2% and the delta read `+1 introduced / −1
resolved` naming the same id in both lists. Now keyed on `changeKind` plus the box (it is textless),
which is what tells two regions apart.

**3. A regression that is really a RE-PAIRING says so** (`delta.repaired`, printed under the
REGRESSION line). Session 18's cry-wolf was not a matcher upgrade — the recorded case — but an
app-side layout change: taller rows re-paired the rail column. The signature is computable and was
present in that very delta: five findings about the text "6" resolved while one presence finding
about "6" came back. So the report now names, beside each regression, the property findings the same
run resolved about the same element text.
Deliberately a DIAGNOSIS, not a reclassification: a genuine vanish has the same shape (the element
goes, its property findings resolve with it), and quietly dropping such a regression would trade one
cry-wolf for a missed one. It fired on its first real run, on the toolbar ghost pair's
`arrow_right_alt`, naming six resolved findings.

**4. `borderStyle` — dashed against solid** (`types.ts`, `adapters/extract.ts`,
`adapters/figma-tree.ts`, `structural/checks.ts`). Session 18 shipped a whole language of dashed
footprints, pills and chips and could not verify a single dash: **403 findings said nothing about
dashedness.** The DOM side reads `borderTopStyle` where a border actually paints; the Figma side maps
`strokeDashes` — the REST field name verified against the recorded `nodes-alert-set` /
`nodes-button-fill-set` fixtures, which carry `"strokeDashes":[10.0,5.0]`, not guessed. Compared
only where BOTH sides declare it and both borders paint: an adapter that cannot read dashes leaves
it undefined rather than defaulting to `solid`, which would report every dashed comp element as
impl-only dashedness.
It reports **zero findings on the corpus** — and the positive control is what says the channel is
live rather than inert: the fresh trees carry `dashed` on both sides of six pairs (9 design / 6 impl
on the desktop ghost). It also retroactively verified session 18's own work: the ghost pill now
measures `dashed`, 1px, radius 9, at 199×30 **on both sides**, where a crop was the only evidence.

**5. WHERE the findings are** (`package/regions.ts`, `report.byRegion`, a `by region:` block in the
run log). A converged loop on a 150-finding pair hand-rolled the same bucketing script twice in one
session to learn that the rail held 106 and the canvas 70 — which is the whole orientation, and a
severity-sorted list shows neither. Each finding is grouped under the SMALLEST captured container
that holds its box: smallest, not first, because containers nest and the outermost holds everything.
A container covering more than 70% of the frame is refused for the same reason ("the whole page" is
not a place), and one smaller than 64px is not a region. On the desktop ghost pair it reads:

```
by region:
    36 findings in surface at (1039, 86) 321×734 — 8 critical, 23 major, 5 minor
    24 findings in image at (-138, -157) 680×740 — 3 critical, 20 major, 1 minor
    12 findings in surface at (45, 86) 497×734 — 4 critical, 7 major, 1 minor
    11 findings in surface at (1040, 660) 320×160 — 2 critical, 6 major, 3 minor
     4 findings in surface at (542, 86) 498×734 — 0 critical, 4 major, 0 minor
    63 in no single region — page chrome, off-frame content, a box larger than every region, or alone in one
```

The `elsewhere` line says what is true rather than what is tidy: on a panned canvas most of those
63 are off-frame content, so calling them "chrome" would have been a comfortable lie.

**6. SVG content is extracted** (`adapters/extract.ts`). An `<svg>` was atomic at any size, so
anything drawn inside a large one was invisible to the structural channel — which is exactly how
session 18's footprint shipped painting NOTHING. Now: an svg stays one atomic `icon` when its shapes
are all icon-sized (≤64px, the Figma side's own `MAX_ICON_PX`) or when it holds more than 24 of them
(a drawing is a picture, not a set of elements — and it keeps the matcher's design × impl candidate
pass from exploding). A large SPARSE one is walked, and its `rect` / `circle` / `path` / `text`
children emit as `role: "shape"`.

- **Decided by the SHAPES, never the svg's own box.** The first attempt gated on the container size
  and changed nothing: a mark layer is a 1×1 px svg with `overflow:visible` whose rects are hundreds
  of px wide. Measured, then fixed.
- **The paint mapping is the Figma adapter's**, so all three sides speak one vocabulary: `fill` →
  backgroundColor, `stroke` → borderColor + borderWidth, `stroke-dasharray` → `borderStyle: dashed`,
  `rx` → borderRadius. A fill that is a PAINT SERVER (`url("#hatch-critical")`, a gradient) goes to
  `backgroundImage` — captured, never compared: `colorDelta` cannot parse it and would silently
  compare nothing, and it would sit in `presenceIdentity` as a suppression key made of an element id.
- **It skips the decoration hoisting.** A shape that paints nothing is not asking its ancestors for a
  background, and walking up from inside an overlay would hand it the pane's.
- **Its own role, like `surface` before it.** A channel that makes new things visible makes new
  noise, so a pair can switch it off with `roles: ["shape"]` without switching off every box.
  Nothing in the matcher or the checks reads a role except `backdrop`, so it costs no pairing
  quality. A Figma design's vectors keep their older `box` / `icon` roles, so that switch silences
  the DOM side only.

**Falsified against the very defect it was built for.** `page.evaluate` bodies have no unit-test
home in this repo (`dc-html-scope.test.ts` uses a fake Page; the surface channel was proved by
reverting a layout and re-running), so the proof is the corpus, in two parts. Positive control: the
ghost footprint is now captured as
`{"role":"shape","box":{"x":687.25,"y":425,"w":208,"h":56},"style":{"backgroundImage":"url(\\"#hatch-critical\\")","borderWidth":1.5,"borderColor":"rgb(229, 72, 77)","borderStyle":"dashed","borderRadius":6}}`
— dashedness, stroke, radius and hatch, none of which any channel could see yesterday. Falsification:
re-introducing session 18's exact class collision (`.gfoot` → `.ghost`, inheriting `opacity:0`) makes
that element **vanish from the impl tree** (2 → 1 rects of 208×56, 216 → 215 leaves). The defect that
was invisible in every artifact the harness produces is now visible in `elements.json`.

**What it costs, measured, and left as the owner's call.** Set 396 → **422**, four pairs moving
(`compare-desktop` 79 → 89, `compare-mobile` 13 → 19, `compare-mobile-minimal` 31 → 36,
`compare-mobile-toolbar` 12 → 17) and the two ghost pairs not moving at all. Of the 26: **14 are
`extra-element` on `shape`s and they are DATA** — the app's comment layer (`--open` purple, `--done`
green) drawn where the comps draw their own demo comments. The other 12 are knock-on: the shapes
joined the candidate pool and re-paired some TEXTLESS elements, one nonsensically (a 99×24 orange
shape against a 16×16 design box → size + colour + spacing). The unrelated-text veto cannot reach
that class — it needs text on both sides — so a **textless equivalent is the next candidate**.
The policy rule for the 14 is deliberately NOT written here (`roles: ["shape"]`, a `regions` entry,
or a fixture whose comments sit where the comps' do): it is the same open decision the bindings
already carry for the artboard surfaces.

**And it exposed something worth knowing: the artboard acceptance is fragile.** Its `contents: true`
hangs off the app's artboard IMAGE being reported as an `extra-element`, which only happens when that
image fails to pair. On the ghost pairs the canvas is panned, the images do not pair, the rule fires
and everything textless inside them is excused; on `compare-desktop` the images DO pair, nothing
fires, and the same shapes are reported. That is why two pairs are quiet and four are not.

**7. Selecting a finding ZOOMS TO it** (`view-math.ts`, Mato's decision). Not a harness improvement
but the product question the ghost pairs kept pointing at, so it belongs in the same record.
`focusView` used to pad by a third of the pane on every side and then clamp the zoom to
`max(current.z, 1)`, and the two together meant it never magnified anything: measured on a 496×734
pane, every element read 100% from 100%, and a whole-page 50% stepped to a flat 100% whether the
element was a 14px badge or a 608px dropzone. Re-centring did all the work.
The rule now, with the limit named: the element + `FOCUS_CONTEXT_PX` (70) each side, anything under
`FOCUS_MIN_BOX_PX` (60) treated as that size — **that** is what stops a 0×0 comment anchor dividing
into an absurd magnification, not the ceiling — capped at `FOCUS_MAX_ZOOM` (2.2), and it zooms OUT
for an element bigger than the pane. A point 220%, the o1 button 146%, a heading 87%, a full-page
backdrop 60%. `current` and `minZoom` are gone from the signature: the result must not depend on
where you happened to be.
One deliberate divergence from the comps: the pane insets bound the ZOOM as well as the centring.
The comps size from the pane's full height and ignore their own bottom sheet, which can scale an
element to fit space behind it. Being right about that also measured better — the mobile ghost pair
went 93 → **89**, where a faithful copy of the comps' formula had made it 95 → 99 in the trial.
`compare-desktop-ghost` 150 → **115** (0.54 → 0.56 confidence), both zoom pills now read 146%, the
`text reads "100%", design says "146%"` finding is gone, and the artboard-content group that held 24
findings in the region breakdown is gone with it. The six pairs with no selection did not move.
The two ghost pairs churned once — four regressions, all off-frame artboard content re-paired under
the new zoom, two of them carrying the `repaired` diagnosis built earlier in the same session — and
settled to `+0/−0` on the next run.

Deliberately NOT done, with the reason, so nobody re-litigates it:
- **`opacity`** — already captured by the extractor and folded into every colour it reports, so a
  faded element already surfaces as a `color` finding with the alpha in it. A second channel would
  double-report the same cause. (And it would not have caught session 18's `opacity:0` footprint,
  which was invisible because SVG is not extracted at all.)
- **`outline`** — Figma has no outline concept, so only one adapter could ever produce it and every
  comparison would be one-sided.
- **`backgroundImage`** — a `repeating-linear-gradient` hatch has no Figma equivalent; presence-only
  comparison is possible but asymmetric enough to want its own decision.
- **`borderStyle` in `presenceIdentity`** — it would make a one-sided box's identity finer (a dashed
  and a solid box are different decisions), but it also re-keys every recorded `accepted` presence
  rule and lapses those suppressions. Separate change, separate blast radius.
- **A TEXTLESS unrelated-pairing veto** — the 2026-09-03 veto needs text on both sides, and SVG
  extraction produced the first clear textless mispairing (a 99×24 orange shape against a 16×16
  design box). The evidence would have to be paint/size counterparts elsewhere rather than text.
  Next candidate.
- **The policy rule for the 14 new `shape` findings** (the app's comment layer over the canvas):
  `roles: ["shape"]`, a `regions` entry, or a fixture whose comments sit where the comps' do. Not
  the tool's decision, and the same open question the artboard surfaces already carry.
- **Comparing paint servers** (`backgroundImage`) — captured now, uncompared: a
  `repeating-linear-gradient` hatch has no Figma equivalent, so presence-only comparison is
  possible but asymmetric enough to want its own decision.

## Session 18 (2026-09-02) — the ghost, built

All in `packages/annotator/src/render.ts` (+ its test), measured on
`refdiff-compare-desktop-ghost` first because its steps contain **no text match at all**.

**What landed**, and the measurement that says so:

- **The footprint** — `ghostRect()`: the element's box padded by 4, radius 6, 1.5px dashed in the
  severity colour, filled with a 45° hatch at 0.18 on a 9px period. An SVG `rect` in `layers[side]`
  (the world layer) so it SCALES with the canvas, with the hatch as three `<pattern>`s in
  `userSpaceOnUse` units. The patterns live in their OWN `<svg id="hatch-defs">`, not in a mark
  layer: `renderMarks()` `replaceChildren()`s those and would wipe a `defs` kept there.
  `rotate(-45)`, not `rotate(45)` — SVG rotates clockwise, so +45 leans the stripes the opposite
  way from the comps' `repeating-linear-gradient(45deg, …)`.
- **The pill** — `ghostPill()`: a div in `markLayers[side]` at a constant screen size, anchored on
  the footprint's top-left corner and lifted its own height + 7 WORLD px
  (`transform-origin:0 100%; transform:translateY(calc(-100% - 7px)) scale(var(--cs))`, the comp's
  own transform). Measured against the comp at its 146% canvas zoom: surface **199×30 ↔ 199×30**,
  the hollow chip's ink **12×13 ↔ 12×13**, the label "Missing here — exists in design"
  **154×14 ↔ 154×14**, and no colour, size or typography finding left on any of them. The 7px gap
  measured 10.2 screen px at 146% on both sides.
- **The halo** — `.vmark.one-sided`: `outline:1.5px dashed <sev>` at `outline-offset:2.5px` on the
  home-side badge of **every** one-sided finding, not only the selected one. That is what the comp
  does (`outline: f.oneSide ? … : 'none'`, unconditional), and the handoff's "of the selected
  finding" was half a rule. Outline is not extracted, so no channel measures it directly — the
  pixel channel sees it indirectly (`compare-mobile`'s canvas region moved on the four haloed
  badges alone).
- **The row chips** — `.fside`: `add_location_alt` + Design only / `wrong_location` + Impl only,
  1px dashed in the severity colour, radius 999, 10px/700 at 0.04em, padding 2px 7px, gap 4,
  glyph 12px. Measured **99×19 / 86×19** against the comp's 99.4×19 / 85.6×19, on all four
  one-sided rows. The caps come from `text-transform`, because **the extractor reads the RENDERED
  text**: the comp's DOM says `Design only` and its capture says `DESIGN ONLY`. They wrap to their
  own line under a long title exactly as the comp's do — `.fhead` is `flex-wrap:wrap` on both
  sides, which is also why the app's selected row grew 134 → 160.
- **The switch** — the pill's `.gsw` (`swap_horiz` + View design / View impl) is always in the
  markup and shown by CSS only under `body.single`, which is the comp's `canSwitch === full mode`.
  It is CSS and not a render-time flag because `setLayout()` re-renders no marks, so a flag baked
  in at render time goes stale on the Split / Full toggle. Selecting a row still never swaps the
  pane by itself — the rejection stands and the test still pins it.

**Two defects that no channel could see**, both found by cropping `impl.png` at the footprint's
screen box and putting it beside the same crop of `design.png`:

1. The rect was classed `ghost`, which this file already spends on the diff lab's superimposed
   design IMAGE (`.ghost { opacity:0 }`) — so the footprint painted NOTHING while the DOM, the
   computed fill and the stroke all read correct. The classes are `.gfoot` / `.gpill` now, and the
   `.ghost` rule says what it is. A `getComputedStyle(el).opacity` probe named it in one call once
   the crop had shown there was something to look for.
2. The hatch leaned the wrong way (`rotate(45)` vs the CSS gradient's 45deg).

The footprint is invisible to the structural channel (SVG is not extracted) and its design-side
counterpart travels suppressed inside the artboard `accepted … contents: true` region, so nothing
is reported about it either way — this is the case where one crop is the whole evidence.

**The zoom-on-select divergence, trialled and left alone.** The comp's canvas reads **146%** after
the step selects `o1`; the app's reads **100%** (`text reads "100%", design says "146%"`), so
everything the canvas draws sits at a different scale on the two sides — ~70 of the desktop ghost
pair's findings have a box inside the canvas, and the pill's own residual is exactly that offset.
The rules disagree by design: the comp's `focusOn` fits the box plus 70px each side, zooming IN to
2.2×; `focusView` (`view-math.ts`) fits it into a third of the pane and then clamps `z` to
`max(current.z, minZoom)`, so it never zooms in — while its own doc says "at most what fits the box
into a third of that area", which the clamp defeats. Trialled in a separate `--out out/vc-trial`
with the comps' formula and REVERTED: `compare-desktop-ghost` 155 → **111**,
`compare-mobile-toolbar-ghost` 95 → **99** (the phone's pane insets leave the comp's 1.23× short at
1.15×). Worth ~44 findings on one pair, −4 on the other, and it changes what selecting a row does
in every pair. **Product decision — ask Mato.**

**One REGRESSION line on `compare-desktop`, and it is a pairing artefact.** `missing-element` on the
design's rail badge "6": taller app rows re-paired the column, so the design's "6" (y=787) lost the
app's "element" prop text (y=813) it had been wrongly paired with — five findings about that element
resolved in the same run. The app's own "6" badge is at y=583, **204px away**, four pixels past the
200px repeated-text cutoff. Its ledger `resolvedAt` is older than the run, and the next run came
back `+0/−0` with no regression line, exactly as SKILL.md §4 predicts for a re-pairing.

## What's DONE

### The Mobile Toolbar layout (`refdiff-compare-mobile-toolbar`, PASS → now 12 with surfaces)
- `packages/annotator/src/render.ts`: `?layout=toolbar`, `toolbarOn()`, body carries
  `layout-minimal` AND `layout-toolbar` (it IS minimal plus toolbars, so every minimal rule
  and `paneInsets` inset applies). Only deltas live under `.layout-toolbar`.
- Measured, not read off markup: Compare segment is IN the header (`Off` x=109 … `Diff`
  x=285), the Show segment is a FLOATING PILL over the canvas (absolute, canvas-relative
  8,8, **223×29**, `--bg1`, 1px `--line`, radius 10, shadow `0 4px 16px`) — reusing
  `.view-panel` (already inside `.work`, already absolute, already in `paneInsets`) with its
  nested `.seg` flattened. **The tool strip STAYS AT THE BOTTOM** (y=807).
- Two wrong turns worth not repeating, both pinned by `test/render.test.ts`: the "top
  floating toolbar" is the Show ROW, not the tool strip (a stub moved the tools to top:8 and
  measured y=127 against the comp's y=807); and `tb-left` must be `flex:0 0 auto` (minimal's
  `flex:1 1 0` left it 99px against the comp's 55 and pushed the auto-centred segment 18px).

### Four core capabilities (after two misses the harness structurally could not see)
1. **Surface channel** — a container that PAINTS emits as `role:"surface"` on BOTH sides
   (`adapters/extract.ts`, `adapters/figma-tree.ts`); `boxShadow` captured for the first
   time. Does NOT double-count the existing decoration hoisting: a container whose paint a
   descendant leaf already carries is `claimed` and stays unemitted (the rule
   `figma-tree.test.ts` pins as "Container with children and decoration is not itself a
   leaf"). Surfaces are excluded from BOTH sides of the design-quality ratio, so the
   `figma-low-quality` gate is untouched (recorded Button/Fill set still scores 0.64).
   Falsified: reverting the toolbar layout to the full-width bar reported
   `extra-element — implementation renders surface at (0, 111) (390×35)`; before, nothing.
2. **Whole-frame remainder backstop** — `diffRemainder` in `pixel/diff.ts` +
   `remainderFinding` in `pixel/checks.ts`. Frame diffed once, every matched box subtracted
   with an AA margin, remainder clustered, reported above 0.4%. Stayed below its floor on
   the falsification (0.28%) because the surface channel caught the bar structurally first —
   correct layering.
3. **Role- AND type-scoped `textPatterns`** — `{ pattern, role?, types? }`; bare strings
   unchanged. **Type** is the axis that mattered: role could not separate the `Review`
   collision because both elements were `role:"text"`. `COMPARE_IGNORE` is now scoped, which
   released 4 real findings the artboard rule had been hiding (an 84px shift among them).
4. **Hidden-movement warning** — `policy-audit.ts` (`hiddenMovement`, `movementPx`), printed
   in the run summary: any suppressed `position`/`size`/`spacing` that moved ≥8px, with the
   rule that hid it, plus the `dataSlots` advice when that rule was a text pattern.
   `accepted` rules are exempt — built from the measurement, already read by a person.

### Run numbering
- `ComparisonReport.run?: number` (OPTIONAL — absent on every report written before this,
  which is exactly what `readPreviousReport` finds on disk), `delta.previousRunNumber`,
  `run: (previous?.run ?? 0) + 1` in `package-for-model.ts`. The counter is the previous
  report, so it has the run dir's lifetime. Annotator: `runLabel(d, report.run)` renders
  "Run 47 vs 46", falling back to the timestamp.

### Interaction `steps` — the capability the ghost needed
- `packages/core/src/adapters/steps.ts`: `CaptureStep` = `{click}|{clickText}|{press}|{wait}`,
  `readStep`/`readSteps`, `stepsOnOneSide`, `describeStep`, `stepHint`, `runSteps(page, …)`.
- Wired into `dc-html.ts`, `live-url.ts`, `storybook.ts` — after `FREEZE_CSS`, before the
  scope resolves (a step may be what brings the scoped node into existence). `steps` on the
  three source types in `pipeline.ts`; `manifest.ts` reads them on both sides;
  `CaptureError` gained `step-failed`.
- **Proven:** the toolbar comp extracts 72 leaf elements by default and **215** after
  `[{clickText:"list_alt"},{wait:400},{click:"[data-vc-step=o1]"},{wait:500}]`.
- **Hard-stop falsified:** breaking a selector deliberately gives
  `{kind:"step-failed", step, index, detail:"target not found"}` at **exit 2** — never a
  quiet capture of the default state.
- Mato added **`data-vc-step="{{f.id}}"`** to the rail row template in BOTH comps at our
  ask, so rows are selectable by identity: the one-sided rows are `o1`..`o4`.

### Fixture parity
- `fixtures/make-demo-root.ts`: a `one()` helper (a one-sided finding carries exactly ONE
  box — `f()` sets both, which would make every row two-sided) plus the comp's rows 12–15:
  `o1` critical design-only "Retake photo button missing" `box(332,586,200,48)`, `o2` major
  design-only `box(476,136,26,26)`, `o3` major impl-only `box(540,16,104,30)`, `o4` minor
  impl-only `box(36,648,608,3)`. Comment `c4`, `side:"design"`, a 0×0 POINT at (618,618) —
  `note()` gained a `side` param. Library card counts moved with them (2/2/2 → 3/5/4,
  comments 3 → 4): the card and the rail are separate declarations and nothing type-checks
  the agreement.
- It paid: `compare-mobile-toolbar` 0.92 → **1.00**, `compare-mobile` 0.91 → 0.97,
  `library-mobile` → 1.00. Parity is what supplies anchors (§1a).

### Smaller, durable
- `packages/annotator/test/pair-coverage.test.ts` — every comp in `design/refdiff/` has a
  manifest pair and vice versa; `UNPAIRED_BY_DESIGN` waives `RefDiff Mobile.dc.html` with the
  reason. It was RED on the real defect before the toolbar pair existed.
- Rail fixes: presence findings on textless elements carry paint+size as identity (four
  surfaces used to share the key `missing-element|surface|[]|[]`, so one snooze hid all
  four); chip counts now promise what enabling them shows (`sevCount`/`triCount`).
- `skills/refdiff/SKILL.md` gained **§0 "A surface that does not exist yet"** (stub →
  register the pair → expect a big first report → converge) and greenfield triggers in its
  `description`.
- `policy.ts` had a committed **raw NUL byte** (`DATA_MASK`) that made the file binary to
  `grep`; now the escape `\u0000` written literally.

## What REMAINS (in order)

### ~~1. The zoom-on-select divergence~~ — DECIDED AND DONE (2026-09-03, Mato)
Selecting a finding now ZOOMS TO it: the element + 70px of context each side, anything under 60×60
treated as 60×60, capped at 2.2×, zooming OUT when the element is bigger than the pane. The insets
bound the zoom as well as the centring — a deliberate divergence from the comps, which size from
the pane's full height and ignore their own bottom sheet. `compare-desktop-ghost` 150 → **115**,
`compare-mobile-toolbar-ghost` 93 → **89**, both zoom pills now read 146%, and the six pairs with
no selection did not move. Constants and the reasoning live in `focusView`
(`packages/annotator/src/view-math.ts`); the bindings carry the decision.

### 2. The comp's rail ROW ORDER (gap 32) and its two `cause` lines (gap 26)
These are what is left of the ghost pairs and most of `compare-desktop`'s 89, and neither is the app's:
the comp's demo array lists 1,2,3,7,8,4,5,6,12..15 and refdiff lists by severity (measured badge by
badge in `elements.json`), and the comp draws a `cause` sentence on two rows from a field the app's
findings do not carry. They shift the app's rail rows against the comp's and re-pair half the
column. The asks are the plan's section H; do not bend the fixture to them.

### 3. The ghost of a one-sided COMMENT — unbuilt and unmeasured
The comps call the same `ghost()` for a selected comment: a 28×28 dashed circle with an 8px hatch
period for a 0×0 POINT anchor, in the status colour, labelled `Comment anchored on design`. No
pair's steps select a comment, so no pair reports it. The app instead MIRRORS every note onto the
other pane, lighter (`.marks.anns .mirror`) — a deliberate 2026-08-28 decision for a different
problem, so mirror-vs-ghost is a design question rather than a parity fix. The fixture already
carries `c4` as a design-side point for whenever it lands; a pair that selects a comment would need
a `data-vc-step` on the comp's comment rows.

### Later / future reference
- `data-vc-step="open-rail"` on the mobile comp's rail button would remove the last
  `clickText` in the manifest (the mobile ghost pair opens the rail by the `list_alt` GLYPH
  name — not user-facing copy, so it is the stable half of a text match).
- **A step that puts BOTH phones on the pane the ghost is on** would make the mobile pill measured
  rather than probe-verified. The comp's phone shows the HOME pane by default, so the ghost sits on
  the pane that is off screen and its pill never enters the capture (that is why the mobile ghost
  pair has no pill in its element tree, while the desktop pair has all of it). A
  `{ clickText: "swap_horiz" }` on both sides of `refdiff-compare-mobile-toolbar-ghost` reaches it
  with the same stability argument as `list_alt` — a glyph name, not user-facing copy.
- **A per-pair `prepare` hook in the manifest** would own the fixture-clock ritual
  (`make-demo-root.ts --now`, measure, restore) that every batch does by hand today.
- Four `missing-element design surface` findings on the compare pairs are the comp's artboard
  drawn as live DOM against the app's PNG — newly visible because those containers are
  textless so `textPatterns` cannot reach them. They want a `regions` entry or an extended
  `accepted` rule. Policy decision, not done.
- `compare-mobile-minimal` reads 4.08% / 36 regions of unexplained remainder (4.10% / 46 before
  SVG extraction accounted for some of it) — real unaccounted area, worth a look.
- The identity key for a one-sided surface includes `w`/`h`, so a resize re-opens any
  decision filed against it. Chosen because three of the comp's cards share a border and
  radius and differ only in size; drop `w`/`h` from `presenceIdentity` if decisions should
  survive resizes instead.
- The minimal comp may also have gained the delta strip — re-fetch and see whether
  `MINIMAL_IGNORE`'s acceptances lapse the way the toolbar's two did.

### Needs research / open questions
- `refdiff-compare-desktop` is at **89** findings (32 before the surface channel, 104 before the
  ghost, 79 before SVG extraction). Session 18 read its rail column badge by badge and named the
  shape — the comp's row order plus the two `cause` lines, which re-pair the column (REMAINS §2) —
  but nobody has read the other findings row by row. It may still contain real app defects, and it
  is the one pair where that is most likely.
- The 0.5px `alignment` note on the toolbar pair (`scale y 0.99937`) is the header's
  `calc(44px + 1px)` against the comp's 44 — decide whether the app or the comp moves.

## How to run

```bash
# build + gates (496 tests: 297 core + 199 annotator)
pnpm build && pnpm typecheck && pnpm test

# dist must be NEWER than src before any measure — see gotchas
pnpm --filter @refdiff/annotator build

# serve the fixture read-only (the measured impl) and the results
refdiff-annotator fixtures/demo-root --serve --read-only --port 7381 --host 0.0.0.0
refdiff-annotator out/refdiff  --serve --port 7382 --host 0.0.0.0     # review UI

# the comps, for hand-measuring an interactive state
( cd design/refdiff && python3 -m http.server 7390 --bind 127.0.0.1 )

# measure — PIN THE FIXTURE CLOCK, measure, restore
node fixtures/make-demo-root.ts --now
refdiff compare --manifest design/refdiff.manifest.mjs --design-dir design/refdiff \
  --app-url http://127.0.0.1:7381 --out out/refdiff [--pair refdiff-compare-desktop-ghost]
node fixtures/make-demo-root.ts
refdiff summary out/refdiff
```

## Key facts / decisions

**The ghost spec, measured off the comps (not read off their source).** Severity colour =
`--critical #e5484d` / `--major #f5a623` / `--minor #4c9aff`.

- **Hatched footprint:** `1px dashed <sev>`, transparent background, radius 6px, and
  `background-image: repeating-linear-gradient(45deg, <sev>/0.18 0px, <sev>/0.18 2px,
  transparent 2px, transparent 9px)` — a 2px stripe on a 9px period at 45°.
- **Ghost pill:** 199×30, `1px dashed <sev>`, background `--bg1`, radius 9px, padding 5px 9px.
  - child 1, the **hollow number chip**: 18×18, `1px dashed <sev>`, background `--bg1`,
    **text in the severity colour**, `border-radius:50%`, 10px/700.
  - child 2, the **direction label**: 11px/600 in `--txt`. Exact copy:
    `Missing here — exists in design` / `Only in impl — nothing here in design`.
  - phone only, child 3: `swap_horiz` + `View design` / `View impl`, 10.5px/700, white.
- **Halo** on the home-side mark: `outline: <sev> dashed 1px` on the 28×28 number badge
  (26×26 for a comment). "Ghosts appear only for the selected finding; home-side marks get a
  dashed halo so density never doubles."
- **Row chips:** `add_location_alt` + `Design only` (missing-element), `wrong_location` +
  `Impl only` (extra-element). Row descriptions: "Element exists in the design ref but is
  absent from the impl DOM" / "Impl renders an element with no counterpart in the design ref".

**Why `steps` hard-stops.** A capture of the wrong state is worse than no capture because it
looks like a result. Same reasoning as `blank-render` and `figma-low-quality`.

**Why steps go on both sides.** A state is a state. The comp selected against the app
unselected reports "selected vs not selected" as drift; the CLI warns via `stepsOnOneSide`.

**Why the impl step is `.frow:has(.fside)`.** The app renumbers findings `f1..fn` every run,
so an id selector means "the first finding", not "a one-sided one" — the ghost would not
appear at all on a run whose `f1` happens to be two-sided.

**Source of truth pointers:** `refdiff.bindings.md` (paths, ports, baseline, traps —
READ IT FIRST), `skills/refdiff/SKILL.md` (the loop; §0 greenfield, §1a alignment, §3a
decisions), `docs/lessons-inbox.md` (the two misses and what was built for them),
`docs/plan-annotator-redesign.md` (the redesign phases and gap numbers).

## Env gotchas

- **The `pnpm dev` watcher stopped rebuilding silently mid-session** (a `git stash`/`pop` of
  `render.ts` during an A/B is the suspected cause). Two CSS fixes read as `+0/−0` delta
  because the served shell was 25 minutes stale. **Compare `dist/render.js` mtime against
  `src/render.ts` before believing any measure**, and restart the annotator — its shell is
  rendered at START.
- **No backticks anywhere in `render.ts`'s client block, comments included** — it lives
  inside a TS template literal and one backtick closes it. Broke the build once this way.
- **`pgrep -f`/`pkill -f` match the shell running them.** An `until ! pgrep -f "…cli.js"`
  loop never exits, and `pkill -f "tsc -p tsconfig"` killed the session's own shell. Match by
  PID, or on a marker the command line does not contain.
- **Pin the fixture clock before measuring** (`node fixtures/make-demo-root.ts --now`, then
  measure IMMEDIATELY, then regenerate WITHOUT `--now`). Unpinned, `library-desktop` read 41
  suppressed against the recorded 25 — pure noise. Never commit `--now` output.
- **Re-run `node packages/annotator/scripts/icon-subset.mjs` after any comp refetch** (needs
  network). A glyph missing from the 97-glyph subset renders as its NAME and every finding
  about it is noise. `--check` says whether the list is current.
- Ports on the devbox: 7378 is an unrelated annotator from another checkout; 7380 serves the
  population-registry DS run root; 7381/7382/7390 are the ones above.
- The comps hydrate `dc-runtime` from `support.js` and React from unpkg — offline runs fail
  `hydration-failed`, not silently.
