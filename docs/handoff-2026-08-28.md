# refdiff — Handoff (2026-08-28, current)

Design-vs-implementation harness, branch `main`, repo `/root/refdiff` (Linux
devbox; Mato's Mac has the same layout under `~/Development/refdiff`), pnpm 10
workspace, TypeScript/ESM, Node ≥22. This is the canonical latest handoff; it
replaces `handoff-2026-08-27.md` (long form of the consuming-repo decisions:
`git show 7994eb5:docs/handoff-2026-08-27.md`).

## State of play

Two workstreams. (1) **The annotator redesign** to the RefDiff comps, measured
by refdiff itself — phases 0–5 DONE (`docs/plan-annotator-redesign.md`), the
four dogfood pairs converged at **9 / 50 / 5 / 3 findings**, confidence 0.90 /
0.71 / 1.00 / 0.90, alignment at the identity on three; every survivor is the
comp's demo data or decision D6, listed item by item in phase 5's Numbers.
Phase 6 (Land it) is docs only. (2) **Harness improvements the redesign
surfaced** — `docs/plan-next.md` items 12–16, the live plan, in the order to
do them. Latest commit `7994eb5` (phase 5), NOT pushed — Mato pushes.

## What's DONE

- Harness items 1–11 (sessions 1–12, `docs/plan-next.md` history): policies,
  aggregation, pixel channel + sub-classification, Figma + live-URL adapters,
  variants, the skill, the regression ledger, `refdiff accept`.
- Annotator phases 0–5 (`docs/plan-annotator-redesign.md`, each with its
  numbers): demo root fixture (`fixtures/demo-root/`), tokens + self-hosted
  type, Library route, comparison chrome, review rail, convergence.
  Commits: `2a52f4a` (2), `fc18f9d` (3), `023374e` (4), `7994eb5` (5).
- Phase 5 specifically: the comps' inline styles are content-box, the app is
  border-box — every fixed-size bordered chrome box is now the comp's number
  plus its border (`render.ts`, comment above the reset; `app-shell.ts`);
  finding rows lost a 3px edge the comp never had; `×14` / `×5` accepted.
  `SKILL.md` (Alignment bullet) and `refdiff.bindings.md` carry the lesson;
  `docs/lessons-inbox.md` has one entry to process.

## What REMAINS (in order)

### 1. `docs/plan-next.md` item 12 — regressions must be absent from the previous run ← DO FIRST
Pure fix in `packages/core/src/package/delta.ts` (`findRegressions` gets
`prev.findings`; box required for non-unique text keys), tests that fail
without it, `SKILL.md` delta definition. Full spec in the plan.

### 2. Item 13 — a non-identity alignment on a same-size page is a finding ← DO SECOND
Pure `alignmentNote`, probably a new `FindingType` `"alignment"` (report
vocabulary → `SKILL.md` §1a + "Reading the measurements" + annotator labels),
`align` column in `summary.md`. Full spec in the plan.

### 3. Item 14 — accept the contents of an accepted element — ASK FIRST
Proposal + three questions in the plan (boxes only vs text; `refdiff accept`
flag; manifest-shape propagation). Ask Mato with the concrete options, then
build.

### 4. Item 15 — same-text pairs before nearest-box under a shift — ASK FIRST
Proposal + questions in the plan (`textMaxGamma`, re-measure the other repos'
baselines). Ask, then build, then measure all four pairs AND the consuming
repos' baselines before merging.

### 5. Item 16 — small: `--read-only` serve, gap 18, annotator phase 6
Phase 6 via `/next-phase` when Mato says.

### Later / future reference
- Consuming-repo decisions (unchanged from the 08-27 handoff, decide THERE):
  population-registry's ×0.875 root font-size (`tokens.css:325`; proposal:
  `font-size` on `body`, not `:root`), then re-run both sets; uctoinak-bmad's
  S10/S11 loop edits + tx-picker needs-a-human list (row format, `má doklad`
  tag, chip copy, `+` prefix, disabled CTA colour). Long form: the 08-27
  handoff in git.
- Migrate uctoinak-bmad + population-registry harnesses onto core; npm
  publishing under `@refdiff`; MCP wrapper maybe; manifest-draft discovery
  command.
- Vendor the `.dc.html` comps' unpkg / Google Fonts dependency for offline runs?
- Annotator: re-anchor a `stale` note by hand; multi-reviewer is
  last-write-wins by design; the comps are content-box (a `box-sizing`
  reset in `support.js` would be a runtime change — Mato's information only).

### Needs research / open questions
- The four asks for Mato on the comps' demo data (annotator plan section H):
  row order (gap 32, ≈26 findings), cause lines (26, ≈13), `saveErr` on c2
  (34, 2), the `Pending` chip (24, 6). Each closes findings on the comp's side.
- Text `size` findings Figma vs DOM mix line-height vs content-area height
  semantics; a shared text-metrics model if it ever matters.
- Spacing measures sibling gaps only; padding against a container edge is
  unchecked until a corpus needs it.
- One real same-glyph icon pair at different sizes to confirm the synthetic
  `noise` floor (2.5–3.7 %) on a browser + Figma render.

## How to run

```bash
pnpm install && pnpm typecheck && pnpm test && pnpm build   # 253 core + 156 annotator tests
pnpm dev                                                     # tsc --watch; the linked CLIs run from dist

# --- the dogfood measure (refdiff.bindings.md; sandbox DISABLED for network + servers) ---
svc up annotator                 # refdiff-annotator fixtures/demo-root --serve → 7379 on the devbox (svc ports)
svc restart annotator            # after ANY annotator edit — the shell is rendered at start
node fixtures/make-demo-root.ts --now
refdiff compare --manifest design/refdiff.manifest.mjs --design-dir design/refdiff \
  --app-url http://127.0.0.1:7379 --out out/refdiff
refdiff summary out/refdiff      # protect: 9 / 50 / 5 / 3, conf 0.90 / 0.71 / 1.00 / 0.90, alignment identity on 3
node fixtures/make-demo-root.ts  # committed clock back; `git status` must show no fixtures/ change
```

The consuming repos' baselines (uctoinak-bmad doc-detail 60/122, 22
suppressed; population-registry Button 141 / 13 causes) are in the 08-27
handoff's "How to run" — needed before merging item 15.

## Key facts / decisions

- **Everything durable lives in this repo** (docs/, skills/) — no Claude
  memory files; a fresh session recovers from the repo alone.
  `docs/architecture.md` "Open decisions" is the decision log,
  `docs/research.md` the evidence base.
- **The model is never the comparator**; the pass gate is deterministic;
  suppression stays visible under `suppressed`; a degraded capture is a
  typed `CaptureError`; one bad pair never kills a run (CLAUDE.md).
- **The skill ships with the code** (CLAUDE.md hard rule): any behaviour
  change updates `skills/refdiff/SKILL.md` in the same change; the skill
  stays repo-agnostic, repo specifics go in `refdiff.bindings.md`.
- **Measure, never eyeball**: every claim is a number from `findings.json`
  (`expected`/`actual` first, crops second); read `alignment` before any
  finding — and read the TRANSFORM, not only `confidence` (phase 5).
- `accepted.ts` REFUSES `position` / `spacing` rules (an offset lapses on
  every recapture) and textless rules without a handle — the reason items
  14 and 15 exist rather than a broader `accepted`.
- FP mandatory: pure stages beside thin adapters, typed `Ok | Err`; every
  pure module has a `*.test.ts`; a bug-fix test must fail without the fix.
- One world space = impl CSS px (the aligned frame) for every box; only the
  PNGs need per-side transforms.

## Env gotchas

- The CLIs run from `dist`: no `pnpm dev` / `pnpm build`, no effect. The
  running annotator never re-reads dist: `svc restart annotator`, then
  `curl <app-url>/ | grep <new-rule>` before an "after" measure.
- The Bash sandbox blocks outbound network and kills backgrounded servers —
  captures, comp hydration (unpkg + Google Fonts) and `--serve` need it off.
- `out/` is gitignored wholesale; the served root is `fixtures/demo-root/`,
  results go to `out/refdiff/`. Never serve the results root.
- The served app WRITES into the fixture (`annotations.json` / `triage.json` /
  `focus.json`) — `git checkout fixtures/` or regenerate before a measure
  (item 16's `--read-only` removes this trap).
- Port 7378 may be another worktree's annotator (`svc ports`).
- Global pre-push hook blocks pushes to main (`ALLOW_PUSH_TO_PROTECTED=1`);
  never push or commit unasked. Commit style: conventional type, no scope
  ticket, body at column 0, blank line before the `Co-Authored-By` footer.
- A raw U+2028 in a regex literal breaks tsc; no backtick anywhere inside
  `render.ts`'s CLIENT template (comments included).
