# refdiff — Handoff (2026-08-28, current)

Design-vs-implementation harness, branch `main`, repo `/root/refdiff` (Linux
devbox; Mato's Mac has the same layout under `~/Development/refdiff`), pnpm 10
workspace, TypeScript/ESM, Node ≥22. This is the canonical latest handoff; it
replaces `handoff-2026-08-27.md` (long form of the consuming-repo decisions:
`git show 7994eb5:docs/handoff-2026-08-27.md`).

## State of play

Both 2026-08-28 workstreams are LANDED. (1) **The annotator redesign** to the
RefDiff comps, measured by refdiff itself — phases 0–6 DONE
(`docs/plan-annotator-redesign.md`); the four dogfood pairs converged at
**2 / 32 / 0 / 3 findings**, confidence 0.89 / 0.71 / 1.00 / 0.90, alignment
at the identity on ALL FOUR (session 15 found the Library desktop's
`alignment` note: the card `.thumb` 1 px short per row, `app-shell.ts`,
committed `009fb52`); every survivor is the comp's demo data (row order,
cause lines, the chip row). Session 15 then made three product changes at
Mato's request (UNCOMMITTED at the time of writing, see "What's DONE"). Phase 6 (session 14) was docs only: `docs/architecture.md`
"Annotator" rewritten to the built IA + the redesign's decisions in "Open
decisions", bindings / README swept. (2) **The harness items the redesign
surfaced** — `docs/plan-next.md` items 12–16 — DONE and committed
(`8b0bc45` … `36583f9`, session 13). Phase 6 is UNCOMMITTED in the working
tree at the time of writing (latest commit `36583f9`). NOT pushed — Mato
pushes.

## What's DONE

- Harness items 1–11 (sessions 1–12, `docs/plan-next.md` history): policies,
  aggregation, pixel channel + sub-classification, Figma + live-URL adapters,
  variants, the skill, the regression ledger, `refdiff accept`.
- Annotator phases 0–6 (`docs/plan-annotator-redesign.md`, each with its
  numbers): demo root fixture (`fixtures/demo-root/`), tokens + self-hosted
  type, Library route, comparison chrome, review rail, convergence, landing
  docs. Commits: `2a52f4a` (2), `fc18f9d` (3), `023374e` (4), `7994eb5` (5);
  phase 6 pending commit.
- Harness items 12–16 (session 13, committed `8b0bc45`, `b9ef34f`,
  `31525e6`, `3229bf7`, `7b0fda6`, `36583f9`): regressions must be absent
  from the previous run (`delta.ts`); a non-identity fit on a same-size page
  is an `alignment` finding + the `align` summary column;
  `accepted[].contents: true` excuses the textless insides of an accepted
  element; same-text pairs before nearest-box (`match.ts` pass 1b);
  `refdiff-annotator --serve --read-only` (the measured instance runs with it,
  `services.toml`). Per item numbers in `docs/plan-next.md`. uctoinak2
  doc-detail identical before/after (76/144); **population-registry NOT
  re-measured** (not on this devbox).
- Session 15 (2026-08-28), after the thumb fix: (1) the Library's
  `computer`/`smartphone` button REMOVED — it is the comp's design-preview
  switch, not a product control; the width alone picks the layout, the icon
  is excused by content (`LIBRARY_IGNORE.textPatterns`), and the desktop
  confidence reads 0.89 for the lost anchor. (2) The delta strip is closable
  in the regression state too (the comp hides the × there; `accepted`
  `text: "close"` on the compare pairs). (3) Dim did nothing: `renderDiffs`
  declared its sheet reach as `const pad = 4000`, shadowing the `pad(box, n)`
  helper — `pad(r.box, 6)` threw on the first hole; renamed `reach`, a test
  in `render.test.ts` fails on the shadow. Plus `annotator-tailnet`
  (`services.toml`): the read-only demo root on the Tailscale IP only,
  `http://uctoinak-dev.tail31a8b9.ts.net:7390/` (Tailscale Serve is not
  enabled on the tailnet; `--host 0.0.0.0` would be public — no firewall).
  (4) Fit — and focusing a finding — centre in the VISIBLE canvas: the phone's
  bottom sheet is an inset (`paneInsets` in `view-math.ts`; pills and the
  desktop's sibling panels are not), re-fit when the sheet's height transition
  ends if the view is untouched. The comp centres under its sheet, so the
  phone's badges now read `position ×11 (0, −22.4)` — gap 35, left visible
  (section H). (5) The phone hides the "N highlighted differences" pill (it
  sat under the zoom / align pills); the stretch WARNING stays.
  Set: 2 / 32 / 0 / 3, identity ×4.

## What REMAINS (in order)

1. ~~The `refdiff-library-desktop` `alignment` note~~ — DONE session 15
   (2026-08-28): undoing the fit on `elements.json` showed `impl.y − raw.y`
   stepping −1 px at every card row's thumbnail; the comp's `.thumb` is
   content-box `height:132px` + `border-bottom:1px` = 133, the app rendered
   132. `calc(132px + 1px)` in `app-shell.ts`; Library desktop 3 → 2, fit
   `1 / 0,0`, the other three pairs unchanged. Bindings / skill §1a / plans
   updated; `pnpm test` 270 + 162 green. NOT committed.
2. **Re-measure population-registry's Button set** (141 / 13 causes) on the
   Mac with item 15 in — the one consuming baseline session 13 could not run.
3. **`docs/lessons-inbox.md` — three entries to process** (`/lessons`): the
   box-model lesson (already in `SKILL.md` + bindings; memory is its last
   home), the matcher-upgrade ledger churn and the harness-only-affordance
   rule — the last two are ALREADY in `docs/architecture.md` "Open decisions"
   since phase 6, so processing is: confirm, and remove.
4. **Gap 18** — the keyboard-shortcut hint has no drawn home in the comps; a
   design question for Mato, not code (every tool's `title` carries its key).
5. **The four demo-data asks for Mato** (annotator plan section H), each
   closing findings on the comp's side, none on the app's: row order (gap
   32, ≈26 findings), cause lines (26, ≈13), `saveErr` on `c2` (34, 2), the
   `Pending` chip (24, 6). Plus two notes for Mato: the local Tool comp's
   `showDeltaStrip` default is flipped to true (gap 29 — a refetch reverts
   it), and the comps are content-box (`support.js` has no reset).

### Later / future reference
- Consuming-repo decisions (unchanged from the 08-27 handoff, decide THERE):
  population-registry's ×0.875 root font-size (`tokens.css:325`; proposal:
  `font-size` on `body`, not `:root`), then re-run both sets; uctoinak-bmad's
  S10/S11 loop edits + tx-picker needs-a-human list (row format, `má doklad`
  tag, chip copy, `+` prefix, disabled CTA colour). Long form: the 08-27
  handoff in git.
- Migrate uctoinak-bmad + population-registry harnesses onto core; npm
  publishing under `@refdiff`; MCP wrapper maybe; manifest-draft discovery
  command.
- Vendor the `.dc.html` comps' unpkg / Google Fonts dependency for offline runs?
- A matcher/identity version stamp on `resolved-ledger.json` (Open
  decisions) if the item-15 churn recurs.
- Annotator: re-anchor a `stale` note by hand; multi-reviewer is
  last-write-wins by design; a `fonts-not-loaded` capture error (Open
  decisions); "text runs inside a styled parent" in the extractor (Open
  decisions — measure the phantom `border` count on a real set first).

### Needs research / open questions
- Text `size` findings Figma vs DOM mix line-height vs content-area height
  semantics; a shared text-metrics model if it ever matters.
- Spacing measures sibling gaps only; padding against a container edge is
  unchecked until a corpus needs it.
- One real same-glyph icon pair at different sizes to confirm the synthetic
  `noise` floor (2.5–3.7 %) on a browser + Figma render.

## How to run

```bash
pnpm install && pnpm typecheck && pnpm test && pnpm build   # 270 core + 162 annotator tests
pnpm dev                                                     # tsc --watch; the linked CLIs run from dist

# --- the dogfood measure (refdiff.bindings.md; sandbox DISABLED for network + servers) ---
svc up annotator                 # refdiff-annotator fixtures/demo-root --serve --read-only → 7379 on the devbox (svc ports)
svc restart annotator            # after ANY annotator edit — the shell is rendered at start
node fixtures/make-demo-root.ts --now
refdiff compare --manifest design/refdiff.manifest.mjs --design-dir design/refdiff \
  --app-url http://127.0.0.1:7379 --out out/refdiff
refdiff summary out/refdiff      # protect: 2 / 32 / 0 / 3 (the library 2 = chip-row position ×10, search size; the mobile 3 = gap 34 ×2 + gap 35 badges ×11), supp 25 / 66 / 16 / 33, conf 0.89 / 0.71 / 1.00 / 0.90, align 1 / 0,0 ×4 — measure right after --now or the relative times drift (+5 suppressed on the library desktop = clock, not a change)
node fixtures/make-demo-root.ts  # committed clock back; `git status` must show no fixtures/ change
```

The consuming repos' baselines (uctoinak-bmad doc-detail 60/122, 22
suppressed; population-registry Button 141 / 13 causes) are in the 08-27
handoff's "How to run" — population-registry still to re-measure with item 15.

## Key facts / decisions

- **Everything durable lives in this repo** (docs/, skills/) — no Claude
  memory files; a fresh session recovers from the repo alone.
  `docs/architecture.md` "Open decisions" is the decision log,
  `docs/research.md` the evidence base.
- **The model is never the comparator**; the pass gate is deterministic;
  suppression stays visible under `suppressed`; a degraded capture is a
  typed `CaptureError`; one bad pair never kills a run (CLAUDE.md).
- **The skill ships with the code** (CLAUDE.md hard rule): any behaviour
  change updates `skills/refdiff/SKILL.md` in the same change; the skill
  stays repo-agnostic, repo specifics go in `refdiff.bindings.md`.
- **Measure, never eyeball**: every claim is a number from `findings.json`
  (`expected`/`actual` first, crops second); read `alignment` before any
  finding — and read the TRANSFORM, not only `confidence` (phase 5).
- `accepted.ts` REFUSES `position` / `spacing` rules (an offset lapses on
  every recapture) and textless rules without a handle — the reason items
  14 and 15 exist rather than a broader `accepted`.
- FP mandatory: pure stages beside thin adapters, typed `Ok | Err`; every
  pure module has a `*.test.ts`; a bug-fix test must fail without the fix.
- One world space = impl CSS px (the aligned frame) for every box; only the
  PNGs need per-side transforms.

## Env gotchas

- The CLIs run from `dist`: no `pnpm dev` / `pnpm build`, no effect. The
  running annotator never re-reads dist: `svc restart annotator`, then
  `curl <app-url>/ | grep <new-rule>` before an "after" measure.
- The Bash sandbox blocks outbound network and kills backgrounded servers —
  captures, comp hydration (unpkg + Google Fonts) and `--serve` need it off.
- `out/` is gitignored wholesale; the served root is `fixtures/demo-root/`,
  results go to `out/refdiff/`. Never serve the results root.
- The measured annotator is served `--read-only` (`services.toml`, item 16):
  PUTs are refused with 405. A writable serve of the fixture WRITES into it
  (`annotations.json` / `triage.json` / `focus.json`) — `git checkout
  fixtures/` or regenerate before a measure if you ran one.
- Port 7378 may be another worktree's annotator (`svc ports`).
- Global pre-push hook blocks pushes to main (`ALLOW_PUSH_TO_PROTECTED=1`);
  never push or commit unasked. Commit style: conventional type, no scope
  ticket, body at column 0, blank line before the `Co-Authored-By` footer.
- A raw U+2028 in a regex literal breaks tsc; no backtick anywhere inside
  `render.ts`'s CLIENT template (comments included).
