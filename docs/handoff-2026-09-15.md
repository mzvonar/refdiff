# refdiff — Handoff (2026-09-15, current) — matcher / divergent implementations

Design-vs-implementation harness. Repo `/root/refdiff` on the devbox, branch **`main`**, pnpm
workspace, TypeScript/ESM, two packages (`@refdiff/core`, `@refdiff/annotator`). **This is the
canonical latest handoff for the MATCHER workstream** (`packages/core/src/structural/`).
`handoff-2026-09-04.md` stays current for the annotator-redesign workstream and is the long-form
record of sessions up to 21 — read it for anything about the annotator, the comps, or
`plan-annotator-redesign.md`.

The work is governed by **[`docs/plan-divergent-matching.md`](plan-divergent-matching.md)** — read
that first. It is self-contained (problem, witness, repro, five steps, the matcher's five
guarantees) and this handoff does not repeat it.

## State of play

refdiff's matcher is unreliable when the implementation diverges structurally from the comp: on
`messages-accountant-desktop` it matches 42 of 80 design / 106 impl leaves at alignment confidence
0.07, and the report says colour, border, radius and typography about pairs that are not the same
element. Root cause found (the veto needs symmetric positive evidence that divergence makes
unavailable); five-step plan committed. **Steps 0, 1 and 2 are DONE.** Next is **step 3,
container-scoped matching with sequence alignment** — the real fix, and the largest change by far.

The guard it will be measured against now exists:
[`baseline-matching-2026-09-15.md`](baseline-matching-2026-09-15.md) — **34 pairs across two
corpora**, regenerated by one command (`node scripts/baseline-matching.ts`), which reproduces it at
**+0 / −0 on every pair**.

Committed, latest **`4c4bb9b`**, **not pushed**:

```
4c4bb9b feat(core): the matcher reports what it paired, and one command baselines the corpus
29c43ee style: adopt the no-semicolon formatting on the matcher's two files
904eba4 docs: refresh handoff for the matcher workstream, pointed at step 2
a415ff0 docs: record phase 1 done, and correct two baseline numbers that were wrong
164828a feat(core): every finding says what paired its two elements, and flags the pairs nothing vouches for
73cc4a1 style: adopt the no-semicolon formatting on the files phase 1 touches
2c358a2 docs: plan for matching when the implementation diverges structurally
```

Working tree CLEAN. Tests **842 passing — 462 core + 380 annotator**.

## What's DONE

- **Root cause, written up** in the plan. `unrelatedPairing()`
  (`packages/core/src/structural/match.ts`) refuses a pass-2 geometric candidate only on *symmetric
  positive evidence* — each text must have a counterpart on the other side, proving both elements
  have better partners available. When the sides diverge most design elements have no counterpart
  at all, so **the veto is structurally unable to fire in exactly the case it is needed**. Fix
  direction: refuse for LACK of evidence, not on proof of a better alternative.
- **The witness** — `messages-accountant-desktop`: the comp's thread date `Včera` pairs with the
  impl's filter chip `Otázky · 1`, **γ 98.7** (the "70.7 px" quoted in older notes is its position
  OFFSET, not its γ), emitting 6 findings — `position`, `color`, `border`, `border-radius`,
  `typography`, and only LAST `text-content`, the one that gives it away.
- **Plan step 0 `73cc4a1`** — the unevaluable veto widening reverted via
  `git checkout -- packages/core/src/structural/`. Suite green at **445 core + 379 annotator**;
  witness re-captured as **run 14: 225 findings (35 critical / 127 major / 63 minor), 42 matched**,
  alignment `(-10.5, 0.0)px` confidence 0.07 (x 0.50 / y 0.07), all six witness findings alive.
  **That run is the baseline step 1 had to not move.** (The commit itself is the formatting split —
  see Env gotchas; the revert restored committed state, so it had nothing of its own to commit.)
- **Plan step 1 `164828a`** — per-finding provenance + the unverified gate.
  - `Finding.via` (`text` | `slot` | `geometry`), `Finding.gamma`, `Finding.unverified`,
    `Finding.unverifiedReason` in `packages/core/src/types.ts`; `MatchVia` is the shared type
    `ElementMatch.via` now uses.
  - Stamped in `structural/checks.ts` by `withProvenance(via, gamma, o)` over the whole batch a
    check produced, never at each `out.push`, so a check added later cannot forget it. A `spacing`
    finding rests on TWO pairs and inherits the **weaker** (`weakerPairing`): one end paired by
    geometry makes the gap a geometric claim however well the other end is proven. Presence
    findings rest on no pair and carry neither field.
  - The gate is `isUnverified(type, via, alignmentConfidence, minAlignmentConfidence)`;
    `VALUE_FINDING_TYPES` = color / typography / border / border-radius / size;
    `DEFAULT_MIN_ALIGNMENT_CONFIDENCE` = 0.5, the pixel channel's floor, deliberately duplicated
    rather than imported (structural must not depend on pixel).
  - Surfaced in `findings.json` (the fields), `refdiff compare` (`provenanceTag` per line +
    `reportProvenance` summary in `cli.ts`), `refdiff summary` (`RunRow.unverified`, `RunRow.via`,
    an `unver` column, a headline count and the set-wide split in `package/summary.ts`), and the
    annotator's finding row (`viaChipHtml` + `.fvia` / `.fvia.unver` in `annotator/src/render.ts`).
  - **Measured, matching untouched:** runs 15 AND 16 both report **+0 introduced / −0 resolved** at
    **225 findings / 42 matched**. `identityKey` (`package/delta.ts`) reads type/role/expected/
    actual/text and none of the new fields, which is why the delta is +0/−0 rather than 225
    findings re-keyed and every triage decision orphaned.
  - **Witness after:** all six read `via: "geometry"` γ98.7; colour, border, border-radius and
    typography flagged `unverified`; `text-content` deliberately NOT, because "text reads
    Otázky · 1, design says Včera" is the line that reveals the mis-pairing.
  - **Whole run:** 49 text / 3 slot / 71 geometry / 102 resting on no pair; **34 flagged**.
  - Tests +12 → 457 core + 380 annotator.
- **Plan step 2 — the corpus baseline and the harness that regenerates it.** Full write-up (what
  moved, what it measured, what is missing and why) is in the plan's own step-2 block; the parts a
  next session needs at hand:
  - **`ComparisonReport.matching`** — `MatchingStats` in `types.ts`, built by the pure
    `matchingStats()` beside `matchElements`, stamped by `packageForModel`. Leaves offered per
    side, `matched` split text/slot/geometry, `designOnly`, `implOnly`, `vetoed`. OPTIONAL, like
    `run`: every report already on disk predates it, and "not recorded" is not "matched nothing".
  - **`refdiff summary` grew a *Matching* table and a *Findings by type* table**, so the guard is
    the ordinary set summary rather than a side artifact. `RunRow.matching` / `RunRow.types`,
    `SetSummary.matching` totalled over the rows that RECORDED one. Back-compat verified on
    `out/u2-before`: no `matching` block ⇒ the table is simply absent.
  - **`scripts/baseline-matching.ts`** (+ `scripts/tsconfig.json`, `scripts/package.json`, and
    `pnpm typecheck` extended to cover them; `pnpm baseline` runs it). Builds first, warms every
    impl URL, tees the run, writes `docs/baseline-matching-<date>.md`.
  - **The baseline: 34 pairs, 5207 findings, 625 unverified. 1336 of 3212 design leaves matched,
    704 of those matches on geometry alone.** That last number is what step 3 is aimed at.
  - Tests +5 → 462 core + 380 annotator.
- **`fix(annotator)` `33ae336`** — unrelated to the matcher; the mobile focus chip was behind the
  toolbar layout's floating Show panel. Verified in a real 390×844 viewport.
- **Outside this repo:** `/root/svc` `73e5d32` fixes two svc defects that only bite non-human
  callers (missing `XDG_RUNTIME_DIR`; `find_owner` matching `.claude` inside a PATH and orphaning
  every agent-started unit). Committed on `master`, not pushed.

## What REMAINS (in order)

### 1. Plan step 3 — container-scoped matching with sequence alignment ← DO FIRST

The real fix, and the largest change by far. Match CONTAINERS first (both sides already carry box
trees; the flat leaf list is a projection), then match each matched container's children **as a
sequence** — LCS / Needleman-Wunsch over (normalized text, role) in reading order — so reordering,
insertion and deletion come out as themselves instead of as crossings. Fall back to today's flat
passes only for children of containers that did not match.

Done when the witness's six findings collapse to an honest `missing-element` (`Včera`) plus
`extra-element` (`Otázky · 1`), the step-2 corpus matched count does not collapse, and the five
matcher guarantees stay green. Expect the alignment confidence itself to want rethinking once
matching is hierarchical — **resist doing that in this step.**

**How to judge it, concretely.** Re-run `node scripts/baseline-matching.ts` and diff the new
document against `docs/baseline-matching-2026-09-15.md`. The *Matching* table is the instrument:
`geom` should FALL (704 of 1336 matches across the corpus rest on geometry alone today) while
`text` holds, and any pair whose `matched` drops sharply is investigated BEFORE the change is kept —
a refusal adds one row to `d-only` AND one to `i-only`, so the finding count alone cannot tell a
precision win from a collapse. `refdiff summary out/baseline/<corpus>` gives the same tables
without re-capturing.

### Later / future reference

- Plan steps 4 and 5: Lowe's ratio test on geometric candidates; per-container confidence.
  **Ordering matters and is argued in the plan** — step 3 shrinks the candidate set so far that a
  ratio threshold and a γ band tuned today would both need re-tuning after, and step 5 is in
  tension with step 3 (it gates on a GLOBAL confidence, while step 3's point is that geometry
  INSIDE a matched container stays reliable even when the global transform is bad).
- `Finding.ambiguityMargin` is declared in `types.ts` and written by **nothing**. **Step 4 fills
  it**; it exists now so the shape the annotator and summary read was settled once, not twice.
- Step 5 inherits step 1's exemption: "a text-proven pair is not judged by the transform's
  confidence" is the special case that becomes the general rule once confidence is per-container.
- The counterpart-widening idea step 0 reverted may pay once there is a baseline. The bar to beat
  is recorded in the plan: −2 findings, +1 match, witness still alive — i.e. marginal.

### Needs research / open questions

- ~~**Should an `unverified` finding count toward the verdict?**~~ **ANSWERED at step 2 from the
  corpus: yes, leave it. Change nothing.** Across 34 pairs / 5207 findings, 625 are `unverified`
  and **382 of those are gating** (≥ major, unexplained) — and excluding them flips the verdict on
  **zero** pairs. Structural, not luck: the flag only fires below global confidence 0.5, and all 27
  pairs carrying one are failing many times over on evidence that is NOT flagged (closest:
  `settings-accountant-desktop`, 41 gating of which 7 unverified). **Re-ask it at step 5**, when
  per-container confidence makes the flag fire on a well-aligned page's one bad container — the
  first configuration in which the two answers are distinguishable. Full argument in the plan.
- **`uctoinak2` commits — needs Mato.** The `THREAD_PARAM` fix there cannot be separated from
  in-flight messages-redesign work: `messages-page-content.tsx` carries 199+/172− of someone else's
  WIP and one line of mine, and `git add` is per-file. Four files ARE wholly mine
  (`src/test/test-db-config.ts` + its test, `tools/design-compare/manifest.mjs`,
  `tools/design-compare/seed-messages-fixtures.mjs`). Options put to Mato: commit only those four,
  one WIP commit for everything, or leave it. **Unanswered.**
- `.next-design/` in that worktree is untracked build output from the capture server — probably
  wants gitignoring, not committing.
- Two **owner** Messages pairs in uctoinak2 point at `/app/org/studio-lumo/messages`, but
  `seed-live-members.mjs` makes `__test__owner` a `BUSINESS_OWNER` of `functional-sro` only — those
  pairs capture a 404 page. Unrelated to this plan, not yet filed.

## How to run

```bash
# refdiff
cd /root/refdiff
pnpm typecheck && pnpm -r test   # expect 462 core + 380 annotator
pnpm -r build                    # dist/ is what the linked `refdiff` CLI runs —
                                 # REBUILD before any compare, or you measure the OLD code

# THE GUARD for steps 3-5: both corpora, one document. Builds first by default.
# The annotator lands on whatever port svc gave it — 7379 today, not the 7378 default.
REFDIFF_APP_URL=http://127.0.0.1:7379 node scripts/baseline-matching.ts
node scripts/baseline-matching.ts --only refdiff   # ~2 min; the other corpus keeps its
                                                   # last numbers in the document, dated
refdiff summary out/baseline/uctoinak2              # the same tables, no re-capture

# the witness pair (uctoinak2 side; server + DB already provisioned and isolated)
cd /root/uctoinak2/.claude/worktrees/messages-redesign
export XDG_RUNTIME_DIR=/run/user/$(id -u)   # svc needs it in non-interactive shells
svc status | grep design-live               # ad-hoc unit, port 3210, NEXT_DIST_DIR=.next-design
DC_PORT=3210 pnpm design:compare --pair messages-accountant-desktop
```

**Restarting `design-live` after it dies** (it did, twice — see Env gotchas), from that worktree:

```bash
svc run --name design-live -- env APP_ENV=test NEXT_DIST_DIR=.next-design PORT=3210 \
  DATABASE_URL="postgresql://postgres:postgres@localhost:54330/uctoinak_designcompare" \
  pnpm exec dotenv -e .env.test -- next dev --turbopack
```

The design-live dev server is Next+turbopack and compiles on demand — the first request after a
restart can take a minute; `curl -o /dev/null -w '%{http_code}'` on
`/app/firm/studio-lumo/messages` should return `307` (redirect to sign-in; the capture harness
authenticates itself).

Full repro — reseeding the sandbox DB `uctoinak_designcompare` on :54330, the `svc run`
invocation, why the manifest pins `?thread=request-5f2c0b9a-…` — is in the plan's **Repro**
section.

## Key facts / decisions

- **The witness pair's numbers:** 80 design leaves / 106 impl leaves, **42 matched**, alignment
  `(-10.5, 0.0)px`, **confidence 0.07 (x 0.50 / y 0.07)**, **225 findings** (35 critical / 127
  major / 63 minor), **34 flagged `unverified`**, pairings 49 text / 3 slot / 71 geometry / 102 on
  no pair.
- **Baseline before thresholds.** Steps 3–5 all make the matcher refuse more pairs, and refusal
  converts into missing + extra. Step 2's corpus table is what tells "more precise" from "failed
  differently".
- **The corpus, 2026-09-15:** 34 pairs (5 refdiff + 29 uctoinak2), 5207 findings, 625 `unverified`.
  **1336 of 3212 design leaves matched, 704 of those on geometry alone** — the number step 3 is
  aimed at. It reproduces at **+0 / −0 on every pair**.
- **A pair that fails to CAPTURE leaves no run dir, so it is invisible to `summary`.** The set
  summary renders a complete-looking table over a shrunken corpus, every row in it correct. 20 of
  the 54 declared pairs are in that state today (4 refdiff annotator-surface breakages, 16
  uctoinak2 — 14 needing Storybook, 2 the known owner-route 404s), and the baseline harness names
  them because nothing else can.
- **Instrument before change.** Step 1 came first because a raw finding count cannot evaluate a
  matcher change — the veto widening produced −10/+8 and told us almost nothing.
- **The gate FLAGS, it does not suppress.** A suppressed finding cannot be told from "no difference
  here" — the same bug with its sign flipped. The pixel channel had already settled this next door:
  below its floor it does not go quiet, it emits one finding SAYING it skipped
  (`lowConfidenceFinding` in `pixel/checks.ts`). Measured cost of the alternative on the witness:
  67 of the 225 findings are value findings, so suppressing on confidence alone deletes all 67, and
  suppressing only what the text exemption leaves still deletes 34 a reader could never know had
  been raised.
- **Alignment confidence is evidence about a PAIRING only where the pairing came from geometry.**
  A `via: "text"` pair carries the same string on both sides, so it is evidence about itself and
  the transform played no part in forming it — exempt at any confidence. This is the one place
  step 1 deliberately sharpened the plan's wording ("gate on alignment confidence"). On the witness
  the 67 value findings split **33 text-proven / 1 slot / 33 geometric**, so the exemption is the
  difference between flagging half the set and all of it.
- **`position` and `spacing` are NOT gated.** They carry `via`/γ so a reader can weigh them, but a
  position finding IS the transform's own claim and states its evidence in its own message ("offset
  by (-70.7, 0.5)px" is visibly not drift); a colour delta offers no such tell.
- **`identityKey` must keep ignoring the provenance fields.** It is built from type + role +
  expected/actual + text, deliberately without geometry, so a human triage decision survives a
  recapture. Adding `via`/`gamma`/`unverified` to it would re-key every finding and orphan every
  decision.
- **`messages-accountant-desktop` captures a thread deep link** because comp frame 1c opens thread
  t2 (`messages.dc.html` state `sel: { od: 0, ad: 1 }`); a bare route opens t1 and the whole
  conversation reports twice over as missing + extra.
- Repo design principles that bind this work are in `CLAUDE.md`: the model is never the comparator,
  pure stages with effects only in adapters, one bad pair must never kill a run, and **suppression
  is visible or it does not happen** — which is the house rule the flag-don't-suppress decision
  above is an instance of.

## Env gotchas

- **This box cannot run two dev servers and a capture browser.** 7 GB, of which a warm Next dev
  server holds ~3.4 GB after compiling the uctoinak2 routes. Starting Storybook beside it (to pick
  up the 14 component pairs) OOM-killed the app server mid-corpus — `dmesg -T | grep -i oom` named
  it outright, after a detour through "did the harness break?". A later full run was killed by the
  task supervisor for the same reason while it was ALSO running `pnpm -r build`. Practical rules:
  `free -m` and `dmesg -T | grep -i oom` before theorising; `--no-build` when dist is known current;
  and measure the corpora in separate passes (`--only <corpus>`) — the harness keeps each corpus's
  last numbers in the document, dated, so a split run still produces a whole baseline.
- **`svc down <name>` from a worktree that does not declare that unit can stop a DIFFERENT one.**
  `svc down annotator-tailnet` run from the uctoinak2 worktree stopped that worktree's ad-hoc
  `design-live` instead — the unit I needed. Run `svc down` from the checkout that owns the unit,
  and check `svc status` after, not before only.
- **A child process's errors are on STDERR, and a stdout-only capture reports a clean run.** The
  first version of the baseline harness piped only stdout, so `compare`'s `console.error` typed
  capture failures never reached it and the document said "nothing missing" about a run with four
  dead pairs. Exactly the failure the whole workstream is about — a report that looks complete —
  reproduced in the tool built to detect it.
- **refdiff has NO formatter of its own, but the editing hook in a uctoinak2-rooted session does.**
  Every Write/Edit fires uctoinak2's `PostToolUse` hook — `oxlint --fix` then `oxfmt --write` — on
  the file just written, regardless of which repo it belongs to. refdiff is mid-migration on
  semicolons (36 of 90 source files still carry them, 54 do not), so the output is where the repo
  is going, but a three-line change to one of the 36 arrives as a few hundred lines of punctuation.
  **Commit the reformat separately FIRST** (see `73cc4a1`): back the files up, `git checkout --`
  them, run the two commands by hand from `/root/uctoinak2`, verify formatted-HEAD is
  behaviour-identical (typecheck + the parent's exact test counts), commit as `style:`, then
  restore and commit the real change. Phase 1's diff went from 1163+/594− to 480+/13− doing this.
- **A number written in a handoff is not a measurement.** Phase 1 started from "expect 448 core"
  and "the count must equal 223" — both wrong (445 and 225), because the previous handoff recorded
  run 13's figures, and run 13 still HAD the widening step 0 removes. The plan's own "225 → 223"
  line said so one paragraph away. **Re-measure the baseline, and when recording one, name the
  run it came from.** Both lessons are in `docs/lessons-inbox.md` (2026-09-15).
- **The CLIs run from `dist`.** A source edit is invisible until `pnpm -r build` (or `pnpm dev`'s
  watch). A compare run against a stale dist reports +0/−0 and is indistinguishable from a change
  that did nothing.
- **`svc` needs `XDG_RUNTIME_DIR`** in non-interactive shells. Fixed in `/root/svc` `73e5d32`; on a
  box without that commit, `export XDG_RUNTIME_DIR=/run/user/$(id -u)` before any `svc` call, or it
  dies with "Failed to connect to bus: No medium found" and looks like systemd is broken.
- **The devbox shells get a snapshot env**, so `~/.bashrc` edits do not reach the current session.
- **`pnpm x` becomes `node x`** — a killed wrapper leaves the real worker alive. Verify with
  `ps -eo pid,etimes,args` before starting anything on the same port.
- **Never `git checkout --` a file with someone else's uncommitted work in it.** Done once on
  `uctoinak2`'s accountant Messages page; restored from a full read earlier in the same context,
  which is luck, not a method.
- `/root/refdiff` and `/root/svc` are clean git repos. `CLAUDE.md`: **do not commit unless asked**;
  nothing is pushed. (Phase 1's commits were asked for explicitly.)
- **Three diagnoses were wrong before the real one** (fixture data; a `cacheComponents`/Suspense
  theory; "same text ⇒ trustworthy pair"). Each survived a plausible argument and died on a
  measurement. `docs/method.md` is the standing rule: every claim of progress is a number from
  `findings.json` or the corpus table, never an eyeball.
